
const fs = require('fs');

const differential = require('./differential');
const formater = require('./formatter');
const scheme = require('./scheme');
let ChekSometing = () => {
differential.differential(); // used exports =
formater(); //used module.exports =    // simple way
global.scheme(); // used used global.##### =
};
ChekSometing(); // run our connect
